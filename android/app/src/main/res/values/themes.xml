<resources>
    <!-- The app has no UI besides the permission dialog, so a platform theme is enough. -->
    <style name="Theme.BackgroundOnyxSyncKOReader" parent="android:Theme.DeviceDefault.Light.NoActionBar" />
</resources>
